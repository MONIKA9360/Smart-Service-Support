package com.example.support.entity.enums;

/**
 * Status lifecycle states for service tickets.
 */
public enum TicketStatus {
    OPEN,
    ASSIGNED,
    IN_PROGRESS,
    RESOLVED,
    CLOSED,
    REOPENED
}
