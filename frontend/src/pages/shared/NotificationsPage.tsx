import PlaceholderPage from '@/components/common/PlaceholderPage'
export default function NotificationsPage() {
  return <PlaceholderPage title="Notifications" subtitle="In-app notification panel with read/unread tracking. Polled every 15 seconds via the /api/notifications endpoint." phase="Phase 8" />
}
